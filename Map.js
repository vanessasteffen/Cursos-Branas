const timeUnits = new Map([['second', 1],['minute', 60], ['hour', 3600]]);
console.log(timeUnits);
console.log(Array.from(timeUnits)); //faz a impressao como esta na inicialização
console.log(timeUnits.size);//retorna a qtd de variaveis(elementos) que esta no vetor