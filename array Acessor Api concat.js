//funcao concat

const ooLanguages = ['Smalltalk', 'C++', 'Simula'];
const funcionalLanguages = ['Haskell', 'Scheme'];
const languages = ooLanguages.concat(funcionalLanguages);//ira unir dois array em um só, mas se cham,ar cada um separado ira
//permanecer igual os vetores
console.log(languages);
